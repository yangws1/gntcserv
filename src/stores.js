import { writable } from 'svelte/store'

export const clearJigsaw = writable([null, ]);
export const name = writable("")
export const order = writable("")
export const sanctuary = writable("")
export const clearCMind = writable(0);
export const varypuzzlecount = writable(0);